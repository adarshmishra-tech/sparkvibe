services:
  - type: web
    name: sparkvibe-ai
    env: node
    plan: free
    nodeVersion: "22.16.0"
    buildCommand: |
      npm cache clean --force
      rm -f package-lock.json
      npm install --no-audit --no-fund --force
    startCommand: npm start
